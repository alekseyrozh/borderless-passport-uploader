{
  "name": "borderless-passport-uploader",
  "version": "0.0.0",
  "scripts": {
    "dev": "AWS_PROFILE=borderless sst dev",
    "deploy:prod": "AWS_PROFILE=borderless sst deploy --stage prod",
    "secret:set": "AWS_PROFILE=borderless npx sst secret set",
    "secrets:list": "AWS_PROFILE=borderless npx sst secret list"
  },
  "devDependencies": {
    "@tsconfig/node20": "^20.1.4",
    "typescript": "5.6.2"
  },
  "dependencies": {
    "sst": "3.3.27"
  }
}
